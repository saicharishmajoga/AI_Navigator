<USER_REQUEST>
following is the current complete workflow of single client module of this entire project. make sure everything has to done dynamically, let us complete the project backend step by step slowly to get accurate dynamic result/output following task has to done within the suitable interface what we have in client module of this project 

The project works as an AI Tool Intelligence Platform that continuously discovers, tracks, and analyzes AI tools from trusted sources such as GitHub, Hugging Face, Product Hunt, company blogs, and official announcements. A scheduler periodically triggers web scrapers or API collectors to gather information about newly launched tools, model updates, version releases, pricing changes, and feature additions. The collected data is cleaned, standardized, and stored in a relational database such as PostgreSQL, while important textual information is converted into embeddings using an embedding model and stored in a vector database like ChromaDB. When a user searches for information, the system first retrieves the most relevant documents from the vector database using semantic search, then passes the retrieved content to an open-source LLM such as Qwen or Llama through a RAG (Retrieval-Augmented Generation) pipeline. Instead of generating answers from memory, the LLM creates responses based on the retrieved documents, which improves accuracy and reduces hallucinations. The system also maintains source links, timestamps, and version history so users can verify where the information originated and determine how recent it is. Additional AI agents can classify tools into categories (coding, image generation, video generation, agents, RAG, etc.), compare competing tools, generate summaries, detect trends, and notify users about significant updates. The frontend provides search, filtering, comparison dashboards, update feeds, and personalized alerts, allowing users to quickly discover the latest AI tools and their changes. In summary, the workflow follows this sequence: Data Sources → Data Collection (Scraping/APIs) → Data Cleaning → PostgreSQL Storage → Embedding Generation → ChromaDB Storage → Semantic Retrieval → RAG + LLM Processing → Source-Verified Response → User Dashboard & Notifications, resulting in a free, scalable, and reliable platform for tracking the rapidly evolving AI ecosystem.

make sure above task has to done successfully, dynamically without any type of errors

just tell me before doing this above task just inform me whatever the modifications you will do, i will verify and proceed with yes answer.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-02T12:01:04+05:30.
</ADDITIONAL_METADATA>